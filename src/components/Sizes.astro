---
import { sizes } from "../utils/size";
---

<ul class="flex justify-center gap-2 mt-4" aria-label="Tallas disponibles">
  {
    sizes.map(({ size }) => (
      <li class="bg-[#ddb4fe] rounded-xl px-3 py-1 text-sm font-semibold text-black">
        {size}
      </li>
    ))
  }
</ul>
